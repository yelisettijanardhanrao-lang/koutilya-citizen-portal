document.addEventListener('DOMContentLoaded',()=>{const form=document.getElementById('form'),btn=document.getElementById('submit'),status=document.getElementById('status');const today=new Date().toISOString().slice(0,10);const date=form.elements.executionDate;date.value=today;form.addEventListener('submit',async e=>{e.preventDefault();btn.disabled=true;status.className='status';status.textContent='Generating PDF...';const data=Object.fromEntries(new FormData(form).entries());try{const r=await fetch('/api/pdf/name-difference-affidavit',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});if(!r.ok){let x={};try{x=await r.json()}catch{}throw new Error(x.message||'PDF generation failed')}const blob=await r.blob(),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download='Name_Difference_Affidavit.pdf';document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),2000);status.className='status ok';status.textContent='PDF generated successfully.'}catch(err){console.error(err);status.className='status err';status.textContent=err.message||'PDF generation failed.'}finally{btn.disabled=false}})});